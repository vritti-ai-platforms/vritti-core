import type { RouteObject } from 'react-router-dom';
import { TaxRegistrationsPage } from './TaxRegistrationsPage';

const routes: RouteObject[] = [{ index: true, element: <TaxRegistrationsPage /> }];

export default routes;
