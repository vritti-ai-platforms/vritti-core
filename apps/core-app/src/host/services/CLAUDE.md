# Services Directory (core-app)

Pure functions. No React, no hooks, no state.
NO async/await — return a `.then()` chain.
Import axios from `@vritti/quantum-ui-native/utils`, not from `'axios'`.
Types live in `types/`, never inline.
`export function`, not `export const`.

Read: `.claude/rules/native-service.md`
