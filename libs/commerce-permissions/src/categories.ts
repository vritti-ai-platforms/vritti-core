// Categories permission codes — MUST match the cloud catalog's authored codes exactly.
export const ORG_CATEGORIES = {
  featureCode: 'categories',
  view: 'org.categories.view',
  add: 'org.categories.add',
  edit: 'org.categories.edit',
  delete: 'org.categories.delete',
} as const;
