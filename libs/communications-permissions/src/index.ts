// Barrel — prefer the per-feature subpaths (@vritti/communications-permissions/whatsapp-accounts) in app code.
// Each feature file hosts one object per workspace scope it's exposed in (ORG_*).

export { ORG_WHATSAPP_ACCOUNTS } from './whatsapp-accounts';
export { ORG_WHATSAPP_OTPS } from './whatsapp-otps';
